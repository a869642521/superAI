# ═══════════════════════════════════════════════════════════════
#  内网穿透（ngrok / cloudflared 等）→ 手机用 4G/5G 也能连你本机后端
#  复制为 app/.env.tunnel 后按需填写（不要提交 .env.tunnel 到 git）
# ═══════════════════════════════════════════════════════════════
#
# 1) 本机启动 Nest（3000）、AI 服务（8000）
# 2) 开两个穿透（示例 ngrok）：
#      ngrok http 3000
#      ngrok http 8000
#    把 https 开头的「Forwarding」域名抄到下面（不要末尾 /）
#
# 3) 用 bash app/android.sh 或带 dart-define 的 flutter run，会自动加载本文件
#
# ── 主 API + WebSocket（与 Nest 同一穿透域名）──────────────────
STARPATH_API_ORIGIN=https://xxxx.ngrok-free.app
#
# ── Python AI 服务（第二个穿透域名）────────────────────────────
STARPATH_AI_ORIGIN=https://yyyy.ngrok-free.app
#
# 若 AI 与 API 用同一个 ngrok（需本机 nginx 反代合并端口），可只填 STARPATH_API_ORIGIN，
# 并把 STARPATH_AI_ORIGIN 留空后自行改代码或反代规则（默认仍走 http://局域网:8000 会失败）。
#
# 局域网与穿透二选一：填了 STARPATH_API_ORIGIN 后，不要再在 .env.dev 里写 STARPATH_API_HOST，
# 以免 dart-define 重复、行为难预期。
